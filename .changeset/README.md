# Changesets

Run `npm run changeset` for every user-facing skill change. Choose:

- `patch` for fixes or instruction refinements
- `minor` for a new skill or meaningful new capability
- `major` for breaking behavior or removal

The release workflow maintains the version pull request and creates a Git tag
when that pull request is merged.
